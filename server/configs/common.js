const INTERFACES_ALIAS = '0.0.0.0';

const SOCKET = {
  CONNECTION: 'connection',
  ROOMS: {
    DEFAULT: 'default',
  },
};

const COMMON = {
  INTERFACES_ALIAS,
  SOCKET,
};

export default COMMON;
